pdfplumber>=0.11
requests>=2.31
flask>=3.0
# Read scanned statement pages (e.g. a stamped balance sheet) offline.
# Pure pip install — no system software (no tesseract, no poppler) needed.
rapidocr-onnxruntime>=1.3
